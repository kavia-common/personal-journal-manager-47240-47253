# PUBLIC INTERFACE ENVIRONMENT VARIABLES
# Copy to .env and set as needed. If unset, the app defaults to http://localhost:3001

# For Create React App style
REACT_APP_API_BASE=http://localhost:3001

# Alternative (not used by CRA directly but supported by client fallback)
VITE_API_BASE=http://localhost:3001
